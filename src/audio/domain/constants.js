// Shared audio constants used across the scheduler, exporter, and domain helpers.

export const MIN_PLAYBACK_RATE = 0.125;
export const MAX_PLAYBACK_RATE = 8;

export const MIN_DURATION_SEC = 0.01;

export const BASE_CHANNEL_TRIGGER_GAIN = 0.75;
export const CLIP_GAIN_SCALE = 0.36;
